// The "form" mode has its own inputs/submit button to interact with, so
// the block opts out of the shared hover/tap scale for all modes rather
// than switching behavior per mode.
export const CONTACT_BLOCK_INTERACTIVE = true;
