// ButtonBlock uses the shared entrance/hover/tap defaults from
// blocks/shared/animation-presets.ts as-is (BlockFrame applies them) — this
// file exists so the block's animation behavior has one obvious place to
// change if it ever needs its own, without touching preview.tsx.
export const BUTTON_BLOCK_INTERACTIVE = false;
