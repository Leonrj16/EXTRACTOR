export interface ProductBlockMeta {
  price?: string;
  currency?: string;
  /** Shown struck-through next to price when set. */
  discountPrice?: string;
  description?: string;
  buttonLabel?: string;
}
