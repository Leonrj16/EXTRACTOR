import { Image as ImageIcon } from "lucide-react";
import type { HeroBlockMeta } from "./types";

export const HERO_BLOCK_DEFAULT_META: HeroBlockMeta = { buttons: [] };
export const HERO_BLOCK_ICON = ImageIcon;
export const HERO_BLOCK_LABEL = "Portada (Hero)";
export const HERO_BLOCK_DESCRIPTION =
  "Una portada con imagen de fondo, avatar, título, descripción y botones de acción.";
