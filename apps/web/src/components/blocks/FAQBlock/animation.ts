// FAQ is a static content card, not a clickable target — the accordion
// items handle their own open/close animation (ui/accordion.tsx), so this
// block opts out of the shared hover/tap scale.
export const FAQ_BLOCK_INTERACTIVE = true;
