// ServiceBlock uses the shared entrance/hover/tap defaults as-is (BlockFrame
// applies them) — this file exists so its animation behavior has one
// obvious place to change without touching preview.tsx.
export const SERVICE_BLOCK_INTERACTIVE = false;
