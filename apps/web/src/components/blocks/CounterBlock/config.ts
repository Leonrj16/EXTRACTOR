import { Hash } from "lucide-react";
import type { CounterBlockMeta } from "./types";

export const COUNTER_BLOCK_DEFAULT_META: CounterBlockMeta = { value: 100 };
export const COUNTER_BLOCK_ICON = Hash;
export const COUNTER_BLOCK_LABEL = "Contador";
export const COUNTER_BLOCK_DESCRIPTION =
  "Un número que cuenta hacia arriba al aparecer — para clientes atendidos, años de experiencia, etc.";
