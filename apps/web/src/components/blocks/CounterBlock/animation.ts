// CounterBlock's own count-up (see preview.tsx) is separate from
// BlockFrame's entrance animation — this file exists so the *entrance*
// behavior has one obvious place to change without touching preview.tsx.
export const COUNTER_BLOCK_INTERACTIVE = false;
export const COUNTER_ANIMATION_DURATION_S = 1.5;
